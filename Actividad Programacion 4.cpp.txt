#include "iostream"
#include "string"

using namespace std;

int main()
{
	float A, CUA, CUB;
	
	cout<<"Escribe el valor de A" <<"\n";
	cin>>A;
	
	CUA=(A)*(A);
	CUB=(A)*(A)*(A);
	
	cout<<"El resultado de A elevada a la 2 es " <<CUA;
	cout<<" y el resultado de A elevada a la 3 es " <<CUB;
	return 0;
}
	
	